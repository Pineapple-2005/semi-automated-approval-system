# =============================================================================
# Philippine ID Document Verification API — Python Dependencies
# =============================================================================

# Web framework and ASGI server
fastapi[all]==0.111.0
uvicorn[standard]==0.29.0

# Async task queue (broker: Redis)
celery[redis]==5.3.6
redis==5.0.4

# ORM and PostgreSQL driver (connects to Supabase)
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Object storage client (MinIO / S3-compatible)
minio==7.2.7

# OCR
pytesseract==0.3.10

# Computer vision (ORB template matching, image quality checks)
opencv-python-headless==4.9.0.80
numpy==1.26.4

# HTTP client (webhook / callback delivery)
httpx==0.27.0

# Multipart file upload parsing
python-multipart==0.0.9

# Data validation and settings management
pydantic==2.7.1
pydantic-settings==2.2.1

# Image handling
Pillow==10.3.0

# Date parsing (DOB extraction from OCR text)
python-dateutil==2.9.0

# Fuzzy string matching for name comparison (rapidfuzz replaces fuzzywuzzy)
rapidfuzz==3.9.3

# Celery messaging transport
kombu==5.3.5
